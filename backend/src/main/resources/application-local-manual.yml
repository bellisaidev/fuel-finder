fuelfinder:
  api:
    base-url: https://stg.fuel-finder.ics.gov.uk/api/v1
    oauth:
      client-id: ${FUEL_FINDER_CLIENT_ID}
      client-secret: ${FUEL_FINDER_CLIENT_SECRET}
  ingestion:
    scheduler:
      enabled: false
      cron: "0 * * * * *"   #every minute
      retailer-name: "FUEL_FINDER_API"
